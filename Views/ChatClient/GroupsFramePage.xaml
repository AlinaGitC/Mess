<Page x:Class="AppClient.Views.ChatClient.GroupsFramePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AppClient.Views.ChatClient"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="300"
      Title="GroupsFramePage">

    <Page.Resources>
        <Style x:Key="ChatItemButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                        Background="{TemplateBinding Background}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F5F5F5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SelectedChatItemStyle" TargetType="Button" BasedOn="{StaticResource ChatItemButtonStyle}">
            <Setter Property="Background" Value="#E3F2FD"/>
        </Style>

        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,15,10,5"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="Background" Value="#FAFAFA"/>
        </Style>

        <Style x:Key="ChatNameStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="10,5,10,0"/>
        </Style>

        <Style x:Key="LastMessageStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#777777"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="Margin" Value="10,0,10,5"/>
        </Style>
        <!-- Стиль для кнопки сворачивания -->
        <Style x:Key="ExpandCollapseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <Viewbox Stretch="Uniform">
                                <Path x:Name="arrow" 
                              Fill="#555555"
                              Data="M 0,5 L 10,5 L 5,10 Z"
                              Stretch="None"/>
                            </Viewbox>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="arrow" Property="Fill" Value="#3F51B5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Disabled">
            <StackPanel>
                <!-- Секция групп -->
                <Border Background="#FAFAFA" Padding="5,0">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Button x:Name="GroupsToggleButton" 
                                    Style="{StaticResource ExpandCollapseButtonStyle}"
                                    Click="ToggleSection_Click"
                                    Tag="GroupsContent"/>
                            <TextBlock Text="Группы" Style="{StaticResource SectionHeaderStyle}"/>
                        </StackPanel>
                        <StackPanel x:Name="GroupsContent" Visibility="Collapsed">
                            <Button x:Name="Group1Button" Style="{StaticResource ChatItemButtonStyle}" Click="ChatButton_Click">
                                <StackPanel>
                                    <TextBlock Text="Группа 1" Style="{StaticResource ChatNameStyle}"/>
                                    <TextBlock Text="Содержание последнего сообщения..." Style="{StaticResource LastMessageStyle}"/>
                                </StackPanel>
                            </Button>
                            <Button x:Name="Group2Button" Style="{StaticResource ChatItemButtonStyle}" Click="ChatButton_Click">
                                <StackPanel>
                                    <TextBlock Text="Группа 2" Style="{StaticResource ChatNameStyle}"/>
                                    <TextBlock Text="Содержание последнего сообщения..." Style="{StaticResource LastMessageStyle}"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Секция личных сообщений -->
                <Border Background="#FAFAFA" Padding="5,0" Margin="0,10,0,0">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Button x:Name="PrivateToggleButton" 
                        Style="{StaticResource ExpandCollapseButtonStyle}"
                        Click="ToggleSection_Click"
                        Tag="PrivateContent"/>
                            <TextBlock Text="Личные" Style="{StaticResource SectionHeaderStyle}"/>
                        </StackPanel>
                        <StackPanel x:Name="PrivateContent" Visibility="Collapsed">
                            <Button x:Name="AlexandraButton" Style="{StaticResource ChatItemButtonStyle}" Click="ChatButton_Click">
                                <StackPanel>
                                    <TextBlock Text="Александра" Style="{StaticResource ChatNameStyle}"/>
                                    <TextBlock Text="Содержание последнего сообщения..." Style="{StaticResource LastMessageStyle}"/>
                                </StackPanel>
                            </Button>
                            <Button x:Name="DmitryButton" Style="{StaticResource ChatItemButtonStyle}" Click="ChatButton_Click">
                                <StackPanel>
                                    <TextBlock Text="Дмитрий" Style="{StaticResource ChatNameStyle}"/>
                                    <TextBlock Text="Содержание последнего сообщения..." Style="{StaticResource LastMessageStyle}"/>
                                </StackPanel>
                            </Button>
                            <Button x:Name="AnnaButton" Style="{StaticResource ChatItemButtonStyle}" Click="ChatButton_Click">
                                <StackPanel>
                                    <TextBlock Text="Анна" Style="{StaticResource ChatNameStyle}"/>
                                    <TextBlock Text="Содержание последнего сообщения..." Style="{StaticResource LastMessageStyle}"/>
                                </StackPanel>
                            </Button>
                            <Button x:Name="LeaderButton" Style="{StaticResource ChatItemButtonStyle}" Click="ChatButton_Click">
                                <StackPanel>
                                    <TextBlock Text="Руководитель" Style="{StaticResource ChatNameStyle}"/>
                                    <TextBlock Text="Содержание последнего сообщения..." Style="{StaticResource LastMessageStyle}"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
